## Summary

<!-- What does this change and why? -->

## Checklist

- [ ] `meson compile -C build` succeeds with warnings-as-errors
- [ ] `meson test -C build` passes
- [ ] Formatting applied (`clang-format -i include/*.h src/*.c`)
- [ ] Commits are signed (`git commit -S`)
- [ ] Formatting-only changes are in a separate commit
- [ ] Documentation and tests updated where behavior changed
